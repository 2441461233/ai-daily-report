name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7.0.1

      - uses: actions/setup-node@v7.0.0
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Validate content
        run: python3 scripts/validate_content.py

      - name: Test deterministic pipelines
        run: |
          python3 -m unittest discover -s tests
          node --test tests/fetch_official_priority_sources.test.mjs
          node --test tests/fetch_artificial_analysis.test.mjs

      - name: Lint
        run: npm run lint

      - name: Build production site
        run: npm run build

      - name: Ensure generated report data is committed
        run: git diff --exit-code -- public/data/reports.json
